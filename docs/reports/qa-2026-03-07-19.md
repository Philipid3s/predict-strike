## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Add a `pizzint.watch` Polymarket fallback path so market opportunities still
populate when `gamma-api.polymarket.com` is unreachable from the runtime.

### Specs Reviewed

- `docs/api/openapi.yml`
- `README.md`

### Compliance Summary

| Area | Status | Notes |
| --- | --- | --- |
| Functional requirements | Pass | Backend now tries Polymarket Gamma first, then `pizzint.watch` breaking markets, then bootstrap payloads. |
| Backend tests | Pass | Unit suite passed after adding parser and fallback coverage for `pizzint.watch` payloads. |
| Acceptance tests | Pass | Existing API-contract acceptance suite remained green with no route or schema drift. |
| Documentation | Pass | README and backend env template now document the fallback URL and runtime behavior. |

### Findings

- `backend/src/collectors/polymarket.py` now accepts `pizzint.watch` market
  rows directly by parsing `title`, `market_id`, `latest_price`, and
  `volume24h`.
- `backend/src/services/market_pipeline.py` now passes the configured
  `POLYMARKET_PIZZINT_BREAKING_URL` into the collector so the fallback path is
  explicit rather than hard-coded in the service layer.
- `backend/.env.example` and `README.md` document the new fallback URL and when
  it is used.
- No API response-shape changes were introduced; the existing documented market
  response contract still matches the implementation.

### Verification

- `python -m unittest discover -s tests -p "test_*.py"` in `backend/`
- `python -m compileall src` in `backend/`
- `python -B -m pytest -q -p no:cacheprovider tests/acceptance` from repo root

### Verdict

- [x] PASS
- [ ] FAIL

### Recommended Actions

- If Gamma remains blocked in Vietnam, set `POLYMARKET_GAMMA_URL` to the normal
  base URL and rely on the new `pizzint.watch` fallback path automatically.
- If you want operator visibility into which upstream was used for a given
  market fetch, add a debug field or a debug endpoint that distinguishes
  `gamma`, `pizzint`, and bootstrap payloads.
