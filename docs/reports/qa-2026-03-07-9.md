## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Re-run the QA compliance pass for the alert generation/history storage slice after the backend contract sync.

### Specs Reviewed

- `docs/specs/initial-draft-project-spec.md`
- `docs/specs/README.md`
- `docs/api/openapi.yml`
- `docs/adr/0008-predict-strike-mvp-scope-and-initial-stack.md`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | The backend now evaluates analyst-facing alerts from current market opportunities, persists alert history in SQLite, and exposes the scoped history/evaluation endpoints expected for the MVP alert slice. |
| API contract            | Pass   | `GET /api/v1/alerts` and `POST /api/v1/alerts/evaluate` now match `docs/api/openapi.yml`, including response keys, alert ID serialization, and status compatibility. |
| ADR alignment           | Pass   | The implementation stays within the FastAPI + SQLite MVP posture in ADR 0008 while adding analyst-facing alert history and evaluation. |
| Acceptance tests        | Pass   | Backend and OpenAPI-traceable acceptance suites both pass after the backend contract sync. |

### Findings

#### Passed

- `backend/src/routes/alerts.py` exposes the documented alert history and evaluation endpoints.
- `backend/src/services/alert_pipeline.py` now returns the documented response shapes for both history and evaluation, including `generated_at`, `evaluated_at`, and `created_count` where required.
- `backend/src/models/schemas.py` now serializes `AlertRecord.id` as a string and allows the documented alert lifecycle statuses.
- The alert-focused acceptance checks in `tests/acceptance/test_openapi_yml_acceptance.py` now pass without further modification.

#### Gaps / Deviations

- None observed within the scoped backend/API surface for this pass.

### Verdict

- [x] PASS - implementation matches specs
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Revisit alert de-duplication before notification delivery so repeated evaluations do not create redundant `open` alerts indefinitely.
- Add acceptance coverage for future alert lifecycle transitions if dismissal or resolution endpoints are introduced.
- Re-run this QA flow whenever alert response shapes or persistence semantics change.
