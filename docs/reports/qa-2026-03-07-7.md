## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Run a QA compliance pass on the Polymarket real market ingestion slice and
confirm the market API remains aligned with specs.

### Specs Reviewed

- `docs/specs/initial-draft-project-spec.md`
- `docs/specs/README.md`
- `docs/api/openapi.yml`
- `docs/adr/0008-predict-strike-mvp-scope-and-initial-stack.md`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | The scoped backend now ingests real Polymarket-oriented market data, persists raw market observations separately, and returns model-versus-market opportunities through the existing API surface. |
| API contract            | Pass   | `GET /api/v1/markets/opportunities` remains aligned with `docs/api/openapi.yml`, and previously documented signal and scoring routes remain unchanged. |
| ADR alignment           | Pass   | The implementation stays within the FastAPI + SQLite MVP posture in ADR 0008 while replacing seeded market data with real Polymarket ingestion. |
| Acceptance tests        | Pass   | Backend and acceptance suites pass, including a new project-spec acceptance check that market ingestion persists a `Polymarket` source observation and returns bounded market probabilities. |

### Findings

#### Passed

- `backend/src/collectors/polymarket.py` adds a Gamma-oriented collector with configurable HTTP loading, deterministic fallback data, and geopolitical market filtering.
- `backend/src/services/market_pipeline.py` persists raw `Polymarket` observations separately before computing opportunity outputs.
- `tests/acceptance/test_initial_draft_project_spec_md_acceptance.py` now verifies the project-spec market-ingestion slice by asserting a call to `GET /api/v1/markets/opportunities` persists a `Polymarket` raw observation and returns bounded market probabilities.
- Existing OpenAPI-traceable route and response-shape checks remain green.

#### Gaps / Deviations

- None observed within the scoped backend/API surface for this pass.

### Verdict

- [x] PASS - implementation matches specs
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Add provider-specific acceptance coverage if the project later relies on a narrower Polymarket query contract or a richer market schema.
- Consider exposing or storing additional market metadata such as volume and last update time when the API contract is ready to expand.
- Re-run this QA flow whenever market-ingestion filtering, storage semantics, or the documented API contract changes.
