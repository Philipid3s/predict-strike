## QA Report - 2026-03-12

Prepared By: qa-agent

### Task Reference

Run a pre-wrap QA pass after the dashboard compaction and four-signal risk-weight
rebalance, update acceptance coverage if needed, and confirm the current repo
state with targeted verification.

### Specs Reviewed

- `docs/api/openapi.yml`
- `tests/acceptance/test_operator_dashboard_ui_acceptance.py`
- `frontend/src/App.tsx`
- `frontend/src/App.test.tsx`
- `backend/src/services/risk_engine.py`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | The dashboard now consistently presents the four active source slices only and shows per-signal weights in the feature snapshot. |
| API contract            | Pass   | The risk-score example in `docs/api/openapi.yml` matches the rebalanced four-signal default weights. |
| Acceptance tests        | Pass   | The operator-dashboard acceptance file matches the compacted dashboard UI and targeted acceptance checks pass. |
| Service-level tests     | Pass   | Backend risk scoring and frontend dashboard tests pass with the updated four-signal weight model. |

### Findings

#### Passed

- `tests/acceptance/test_operator_dashboard_ui_acceptance.py` matches the current dashboard structure: four active signal cards, compact source cards, and visible signal weights.
- `backend/src/services/risk_engine.py` now allocates default scoring weight only to OpenSky, NOTAM, GDELT, and Pizza Index.
- `docs/api/openapi.yml` reflects the updated scoring example instead of the retired six-signal weight mix.

#### Residual Risks

- The broader API schema still retains `satellite_buildup` and `osint_activity` fields with zero default weight. That is contract-compatible today, but it can confuse future consumers unless the dormant-feature strategy is documented consistently across future UI/API changes.

### Verdict

- [x] PASS - targeted QA checks passed for the current change set
- [ ] FAIL - blocking deviations remain

### Verification

- `python -B -m pytest -q -p no:cacheprovider tests/acceptance/test_operator_dashboard_ui_acceptance.py` -> PASS (`3 passed`)
- `python -B -m pytest -q -p no:cacheprovider tests/acceptance/test_openapi_yml_acceptance.py` -> PASS (`9 passed`)
- `python -B -m pytest -q -p no:cacheprovider backend/tests/test_risk_engine.py` -> PASS (`4 passed`)
- `npm test --prefix frontend` -> PASS
