## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Improve the Pizza Index Puppeteer scraper so it resolves the intended Google
Maps place, extracts real place-detail fields, and returns meaningful
Puppeteer-backed activity instead of null-heavy fallback output.

### Specs Reviewed

- `docs/specs/pizza-index-google-maps-activity.md`
- `docs/adr/0009-google-maps-restaurant-activity-for-pizza-index.md`
- `docs/api/openapi.yml`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | The live Puppeteer path now resolves Google Maps search results, persists raw payloads, and returns real place-detail metadata for the tested target. |
| API contract            | Pass   | No external endpoint shape changed; only backend extraction behavior improved. |
| ADR alignment           | Pass   | ADR/spec notes now document search-result resolution, accessibility-tree parsing, and closed-state inference. |
| Acceptance tests        | Pass   | `28 passed` in `tests/acceptance/`. |

### Findings

#### Passed

- `backend/tools/pizza_index_puppeteer_runner.mjs` now uses a
  search-result-to-detail flow instead of naïve direct place-slug navigation.
- The live local Puppeteer smoke test for
  `dominos_pentagon_city` returned a real Puppeteer-backed observation with:
  - `provider = puppeteer`
  - `capture_status = puppeteer_detail_scrape_ok`
  - `address = 3535 S Ball St, Arlington, VA 22202, United States`
  - `rating = 2.5`
  - `reviews_count = 414`
  - `is_open = false`
  - inferred closed-state `current_busyness_percent = 0`
  - inferred closed-state `usual_busyness_percent = 0`
- `backend/src/services/pizza_index_pipeline.py` no longer collapses meaningful
  partial Puppeteer payloads into stub responses.
- Backend tests now cover partial Puppeteer preservation and secondary-metadata
  partial classification.

#### Gaps / Deviations

- The verified live smoke result was for a closed place, so the current local
  proof point is a closed-state detail scrape rather than an open/live busyness
  scrape. This is acceptable and consistent with the documented closed-state
  inference rule.

### Verdict

- [x] PASS - implementation matches specs
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Run a second live smoke test during Arlington business hours to verify
  extraction of non-zero current or usual busyness from the Popular Times
  widget.
- Extend the same search/detail logic to the remaining Pizza Index targets and
  confirm each target resolves to the intended monitored location.
