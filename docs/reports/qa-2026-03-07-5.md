## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Run a QA compliance pass on the new NOTAM slice, updating acceptance coverage
where the previous single-source assumption is now stale.

### Specs Reviewed

- `docs/specs/initial-draft-project-spec.md`
- `docs/specs/README.md`
- `docs/api/openapi.yml`
- `docs/adr/0008-predict-strike-mvp-scope-and-initial-stack.md`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | The scoped backend now collects NOTAM-derived signal input alongside OpenSky, persists raw NOTAM observations separately from derived snapshots, and continues to expose the same signal, refresh, scoring, and market-comparison API surface. |
| API contract            | Pass   | `GET /api/v1/signals/latest`, `POST /api/v1/signals/refresh`, `POST /api/v1/risk/score`, and `GET /api/v1/markets/opportunities` remain aligned with `docs/api/openapi.yml`. |
| ADR alignment           | Pass   | The implementation stays within the FastAPI + SQLite MVP posture documented in ADR 0008 while extending the collector set behind the existing API. |
| Acceptance tests        | Pass   | Backend and acceptance suites pass after replacing the stale single-source raw-observation count assumption with a stronger refresh-persistence invariant. |

### Findings

#### Passed

- `backend/src/collectors/notam.py` adds a NOTAM collector with configurable HTTP loading, deterministic fallback data, and a transparent `notam_spike` heuristic.
- `backend/src/services/signal_pipeline.py` now persists raw OpenSky and NOTAM observations separately before writing the derived signal snapshot.
- `tests/acceptance/test_openapi_yml_acceptance.py` now verifies the stronger refresh invariant: a refresh adds raw observations and one new snapshot while preserving the documented response shape.
- Existing API response-shape checks remain green with the added NOTAM source entry.

#### Gaps / Deviations

- None observed within the scoped backend/API surface for this pass.

### Verdict

- [x] PASS - implementation matches specs
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Reuse the current multi-source persistence pattern for future collectors so acceptance tests can keep asserting refresh growth rather than source-count assumptions.
- If a specific NOTAM provider contract is later approved, add provider-specific acceptance coverage without changing the public API shape unless necessary.
- Re-run this QA flow whenever collector count, refresh semantics, or snapshot storage behavior changes.
