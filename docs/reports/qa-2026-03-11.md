## QA Report - 2026-03-11

Prepared By: qa-agent

### Task Reference

Conduct a full QA health check for the current Predict Strike repository state,
comparing implementation and test coverage against docs/specs,
`docs/api/openapi.yml`, and relevant ADRs.

### Specs Reviewed

- `docs/specs/initial-draft-project-spec.md`
- `docs/specs/pizza-index-google-maps-activity.md`
- `docs/api/openapi.yml`
- `docs/adr/0008-predict-strike-mvp-scope-and-initial-stack.md`
- `docs/adr/0009-google-maps-restaurant-activity-for-pizza-index.md`
- `docs/adr/0010-gdelt-doc-2-canonical-news-source.md`
- `tests/acceptance/test_initial_draft_project_spec_md_acceptance.py`
- `tests/acceptance/test_openapi_yml_acceptance.py`
- `tests/acceptance/test_operator_dashboard_ui_acceptance.py`
- `tests/acceptance/test_pizza_index_google_maps_activity_md_acceptance.py`
- `tests/acceptance/test_runtime_modes_acceptance.py`

### Compliance Summary

| Area                    | Status  | Notes |
| ----------------------- | ------- | ----- |
| Functional requirements | Partial | Core MVP slices are implemented for signals, Pizza Index, GDELT detail, risk scoring, markets, alerts, and dashboard flows, but the broader initial project spec still contains planned modules that remain intentionally incomplete. |
| API contract            | Pass    | Current backend/frontend surface aligns with the documented OpenAPI routes and response-shape checks exercised by the acceptance suite. |
| ADR alignment           | Pass    | The present stack and implemented source slices align with ADR 0008, ADR 0009, and ADR 0010. |
| Acceptance tests        | Fail    | `python -m unittest discover -s tests/acceptance -p "test_*.py"` fails on a stale frontend coverage assertion in `tests/acceptance/test_operator_dashboard_ui_acceptance.py`. |

### Findings

#### Passed

- `backend/**` exposes the documented MVP API surface for health, signals, Pizza Index, risk scoring, markets, alerts, OpenSky detail, and GDELT detail.
- `frontend/**` includes the operator dashboard and source-detail flows expected by the current API contract, including per-source status, alert evaluation, Pizza Index detail, GDELT detail, and the OpenSky source workflow.
- The Pizza Index implementation and schemas align with the dedicated planning/spec material in `docs/specs/pizza-index-google-maps-activity.md` and ADR 0009.
- The GDELT detail path is implemented as a dedicated slice consistent with ADR 0010's canonical-source decision.

#### Gaps / Deviations

- Acceptance coverage drift: `tests/acceptance/test_operator_dashboard_ui_acceptance.py:63`-`71` requires the literal frontend-test marker `Source Fallback`, but the source specs do not require that exact phrase. The documented contract in `docs/api/openapi.yml` models fallback as `mode: fallback`, and the current UI/test wording uses `Data mode: Fallback` / `Static Baseline` instead. Observed behavior: the repository acceptance run fails at `tests/acceptance/test_operator_dashboard_ui_acceptance.py:58` because `frontend/src/App.test.tsx:357`-`376` no longer contains `Source Fallback`.

### Verdict

- [ ] PASS - implementation matches specs
- [x] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Update `tests/acceptance/test_operator_dashboard_ui_acceptance.py` so its expected frontend markers match the actual documented fallback contract instead of requiring the stale literal `Source Fallback`.
- Re-run `python -m unittest discover -s tests/acceptance -p "test_*.py"` after that acceptance test is corrected.
- Clean up repository hygiene separately: the current worktree contains tracked/generated `__pycache__/*.pyc` files and mutable acceptance `.db` artifacts, which increases review noise even though it was not the failing spec-compliance gate in this pass.
