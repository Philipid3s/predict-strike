## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Run a QA compliance pass on the new signal refresh job/endpoint and confirm the
collector/storage API remains aligned with specs.

### Specs Reviewed

- `docs/specs/initial-draft-project-spec.md`
- `docs/specs/README.md`
- `docs/api/openapi.yml`
- `docs/adr/0008-predict-strike-mvp-scope-and-initial-stack.md`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | The scoped backend now supports refresh-driven collection on top of the existing OpenSky + SQLite slice, while continuing to store raw observations separately from derived snapshots. |
| API contract            | Pass   | `POST /api/v1/signals/refresh` matches `docs/api/openapi.yml` and the previously documented endpoints remain aligned. |
| ADR alignment           | Pass   | The refreshed endpoint surface remains consistent with ADR 0008 and the current FastAPI + SQLite MVP posture. |
| Acceptance tests        | Pass   | Backend and acceptance suites passed, including new OpenAPI-traceable refresh coverage. |

### Findings

#### Passed

- `POST /api/v1/signals/refresh` forces a fresh collection pass and returns the documented `SignalSnapshot` shape.
- The refresh path persists a new raw OpenSky observation and a new derived signal snapshot without breaking `GET /api/v1/signals/latest`.
- `tests/acceptance/test_openapi_yml_acceptance.py` now verifies both route existence and refresh persistence behavior derived from `docs/api/openapi.yml`.
- Existing scoring, market-comparison, and storage-traceability checks remain green alongside the new refresh coverage.

#### Gaps / Deviations

- None observed within the scoped backend/API surface for this pass.

### Verdict

- [x] PASS - implementation matches specs
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Add a scheduler or worker-triggered refresh path when the project is ready to move beyond manual/API-driven refresh.
- Reuse the refresh pattern for the upcoming NOTAM slice so new collectors can persist observations and snapshots consistently.
- Re-run this QA flow whenever the signal endpoints or snapshot storage semantics change.
