## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Verify the source-provenance labeling slice for the operator dashboard so the UI
explicitly distinguishes `live`, `fallback`, and `static_baseline` data.

### Specs Reviewed

- `docs/api/openapi.yml`
- `frontend/src/App.tsx`
- `backend/src/models/schemas.py`
- `backend/src/services/signal_pipeline.py`
- `backend/src/services/market_pipeline.py`

### Changed Files

- `tests/acceptance/test_openapi_yml_acceptance.py`
- `tests/acceptance/test_runtime_modes_acceptance.py`
- `tests/acceptance/test_operator_dashboard_ui_acceptance.py`
- `docs/reports/qa-2026-03-07-12.md`

### Compliance Summary

| Area | Status | Notes |
| --- | --- | --- |
| Signal-source provenance | Pass | Signal sources now expose `mode` and the snapshot includes static-baseline source entries for non-live indicators. |
| Polymarket provenance | Pass | Market opportunities now expose a dedicated `source` block with explicit live/fallback provenance. |
| Dashboard clarity | Pass | Frontend renders provenance labels on source cards and in the Polymarket section. |
| Regression checks | Pass | Backend tests, frontend tests/build, and acceptance coverage are all green. |

### Findings

#### Passed

- `SignalSource` now includes the required `mode` field with the documented enum.
- The signal snapshot no longer carries ambiguous Polymarket provenance.
- Static-baseline indicators are represented explicitly in the source list.
- The frontend UI exposes provenance labels directly instead of forcing the user
  to infer them from status alone.

#### Gaps / Deviations

- Provenance is explicit at the source level, but the feature tiles themselves
  still do not show feature-to-source mapping. That is acceptable for this slice
  because the requested change was source labeling, not a feature lineage view.

### Verification Run Result

- `python -m unittest discover -s tests -p "test_*.py"` -> PASS
- `npm test --prefix frontend` -> PASS
- `npm run build --prefix frontend` -> PASS
- `python -B -m pytest -q -p no:cacheprovider tests/acceptance` -> PASS

### Verdict

- [x] PASS - implementation matches the scoped source-labeling task
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- If you want full feature lineage later, add feature-level provenance metadata
  so each risk input can be traced to a specific source card.
