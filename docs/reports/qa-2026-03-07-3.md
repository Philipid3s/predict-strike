## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Run a QA compliance pass on the first real collector/storage slice for Predict
Strike, focused on the new OpenSky ingestion and SQLite persistence path behind
the existing API surface.

### Specs Reviewed

- `docs/specs/initial-draft-project-spec.md`
- `docs/specs/README.md`
- `docs/api/openapi.yml`
- `docs/adr/0008-predict-strike-mvp-scope-and-initial-stack.md`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | The scoped backend slice now performs real OpenSky-backed collection with deterministic fallback, persists raw observations separately from derived signal snapshots, and continues to expose explainable scoring and market comparison through the existing API surface. |
| API contract            | Pass   | `GET /api/v1/signals/latest`, `POST /api/v1/risk/score`, and `GET /api/v1/markets/opportunities` remain aligned with `docs/api/openapi.yml`. |
| ADR alignment           | Pass   | The implementation stays within the FastAPI + SQLite MVP direction documented in ADR 0008. |
| Acceptance tests        | Pass   | Backend and acceptance suites passed, including the new storage-traceability acceptance check derived from `docs/specs/initial-draft-project-spec.md`. |

### Findings

#### Passed

- `backend/src/collectors/opensky.py` introduces a real collector abstraction with a live HTTP path and deterministic fallback data for constrained environments.
- `backend/src/storage/signal_store.py` stores raw source observations separately from derived signal snapshots, matching the normalization guidance in `docs/specs/initial-draft-project-spec.md`.
- `GET /api/v1/signals/latest` continues to satisfy the documented response contract while now serving a persisted snapshot.
- `GET /api/v1/markets/opportunities` continues to compute model-versus-market comparisons from the persisted latest snapshot.
- `tests/acceptance/test_initial_draft_project_spec_md_acceptance.py` now covers the requirement to store raw observations separately from derived features.

#### Gaps / Deviations

- None observed within the scoped backend/API surface for this pass.

### Verdict

- [x] PASS - implementation matches specs
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Add a refresh-oriented workflow or endpoint when the project is ready to collect successive snapshots instead of create-once/read-many behavior.
- Extend acceptance coverage when additional real collectors are introduced for NOTAM, news, or market ingestion.
- Re-run this QA flow whenever the signal storage schema or the documented API contract changes.
