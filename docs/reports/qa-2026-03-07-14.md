## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Implement the first real Pizza Index provider path with a Puppeteer primary
collector, quota-controlled SerpApi fallback, caching, persistence, and aligned
API/docs coverage.

### Specs Reviewed

- `docs/api/openapi.yml`
- `docs/adr/0008-predict-strike-mvp-scope-and-initial-stack.md`
- `docs/adr/0009-google-maps-restaurant-activity-for-pizza-index.md`
- `docs/specs/pizza-index-google-maps-activity.md`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | Pizza Index now has a live Puppeteer primary path, SerpApi fallback, target cache, snapshot cache, and a hard daily fallback budget. |
| API contract            | Pass   | `docs/api/openapi.yml` now matches the implemented provider/provider_mode/data_quality semantics and target registry examples. |
| ADR alignment           | Pass   | ADR 0008 now documents Node.js runtime support for browser automation; ADR 0009 records the live provider model and quota-controlled fallback. |
| Acceptance tests        | Pass   | `27 passed` in `tests/acceptance/`, including the new Pizza Index provider-path acceptance file. |

### Findings

#### Passed

- `backend/src/services/pizza_index_pipeline.py` recreates the missing service
  and implements the live provider path, snapshot scoring, and cache/persistence
  flow.
- `backend/tests/test_pizza_index_pipeline.py` and
  `backend/tests/test_pizza_index_routes.py` cover Puppeteer success, SerpApi
  fallback, and hard quota exhaustion behavior.
- `tests/acceptance/test_pizza_index_google_maps_activity_md_acceptance.py`
  traces the new provider-path behavior back to the Pizza Index design spec.
- `docs/api/openapi.yml` examples no longer describe a stub-only Pizza Index
  contract.

#### Gaps / Deviations

- None.

### Verdict

- [x] PASS - implementation matches specs
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Add raw provider-payload persistence if Pizza Index backtesting will depend on
  auditability of each scrape/fallback response.
- Add a browser-environment smoke test once local/dev Docker runtime is used to
  validate Puppeteer dependencies outside mocked unit coverage.
