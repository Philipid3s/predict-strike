## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Debug the live Pizza Index runtime after the PizzINT provider switch and fix the
misleading failure status so upstream connectivity problems are reported
honestly.

### Specs Reviewed

- `docs/api/openapi.yml`
- `docs/specs/pizza-index-google-maps-activity.md`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | Backend source now preserves upstream failure reasons during snapshot refreshes. |
| Acceptance tests        | Pass   | `14 passed` for the Pizza Index/OpenAPI acceptance subset after the observability fix. |
| Live process state      | Watch  | The currently running local backend process still appears to be serving older code until it is restarted or reloaded. |

### Findings

- Direct local CLI fetch to `https://www.pizzint.watch/api/dashboard-data`
  failed from this environment with `Unable to connect to the remote server`.
- The previous refresh path collapsed that failure into
  `pizzint_target_missing_from_dashboard`, which obscured the real issue.
- `backend/src/services/pizza_index_pipeline.py` now preserves the upstream
  fetch failure reason for snapshot/refresh paths instead of masking it as a
  target-missing condition.
- Local unit tests and targeted acceptance tests passed after the fix.
- A live call to `http://localhost:8000/api/v1/pizza-index/refresh` still
  returned the old `pizzint_target_missing_from_dashboard` status, which
  indicates the already-running backend process has not reloaded the new code.

### Verdict

- [x] PASS - repository state is correct
- [ ] FAIL - live local process must be restarted/reloaded to observe the fix

### Recommended Actions

- Restart the local backend process, then call:
  - `POST /api/v1/pizza-index/refresh`
  - `GET /api/v1/pizza-index/targets/dominos_pentagon_city/activity`
- If the machine still cannot reach `www.pizzint.watch`, expect a
  `pizzint_failed:...` capture status rather than a false “missing from
  dashboard” message.
