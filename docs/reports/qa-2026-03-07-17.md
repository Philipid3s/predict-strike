## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Replace the Pizza Index primary provider with the PizzINT dashboard API,
preserve the monitored Google Maps place URLs for fallback use, keep SerpApi as
fallback-only under the daily cap, and align the backend/docs/tests to the new
upstream design.

### Specs Reviewed

- `docs/specs/pizza-index-google-maps-activity.md`
- `docs/adr/0009-google-maps-restaurant-activity-for-pizza-index.md`
- `docs/api/openapi.yml`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | Pizza Index now uses PizzINT dashboard JSON as primary, preserves exact Google Maps URLs, and keeps SerpApi fallback under the hard daily limit. |
| API contract            | Pass   | OpenAPI examples and provider enums now reflect `pizzint | serpapi | stub` and the preserved Google Maps target links. |
| Runtime resilience      | Pass   | Snapshot endpoints now degrade to fallback/stub behavior instead of failing hard when the upstream is blocked. |
| Acceptance tests        | Pass   | `28 passed` in `tests/acceptance/`. |

### Findings

#### Passed

- `backend/src/services/pizza_index_pipeline.py` now uses the PizzINT dashboard
  payload as the primary upstream instead of the local Puppeteer runner.
- The Pizza Index target registry now preserves exact upstream Google Maps place
  URLs for the monitored stores that are mapped to PizzINT.
- Raw provider-payload persistence now keeps upstream fields and normalized
  capture metadata together, so audit/debug fields survive storage.
- Old cached Pizza Index rows using the retired `puppeteer` provider are
  treated as stale and are not reused after the provider switch.
- `backend/tests/` and `tests/acceptance/` both validate the new provider
  semantics and SerpApi fallback behavior.

#### Gaps / Deviations

- `wiseguy_pizza_rosslyn` remains in Predict Strike's target registry, but the
  current PizzINT dashboard payload reviewed during this session does not expose
  that target. The backend handles this honestly via fallback/stub behavior, but
  target-registry alignment remains a product choice to resolve later.

### Verdict

- [x] PASS - implementation matches specs
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Decide whether Predict Strike should keep non-PizzINT targets like
  `wiseguy_pizza_rosslyn` in the registry or align the registry fully to the
  upstream-monitored set.
- If direct-source independence becomes a requirement later, add a second
  collector path for Google Maps/SerpApi comparison rather than replacing the
  preserved upstream-linked target registry.
