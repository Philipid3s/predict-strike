## QA Report - 2026-03-11

Prepared By: qa-agent

### Task Reference

Refresh the QA report after the acceptance-test and CI-contract fixes so the
repo has an up-to-date post-fix QA verdict.

### Specs Reviewed

- `docs/specs/initial-draft-project-spec.md`
- `docs/specs/pizza-index-google-maps-activity.md`
- `docs/api/openapi.yml`
- `docs/adr/0008-predict-strike-mvp-scope-and-initial-stack.md`
- `docs/adr/0009-google-maps-restaurant-activity-for-pizza-index.md`
- `docs/adr/0010-gdelt-doc-2-canonical-news-source.md`
- `tests/acceptance/test_operator_dashboard_ui_acceptance.py`
- `.github/workflows/ci.yml`

### Compliance Summary

| Area                    | Status  | Notes |
| ----------------------- | ------- | ----- |
| Functional requirements | Partial | The implemented MVP slices remain consistent with the current project scope, while broader planned modules in the initial project spec remain intentionally unimplemented. |
| API contract            | Pass    | The backend/frontend contract remains aligned with `docs/api/openapi.yml`, including the fallback/source-mode behavior covered by the updated acceptance test. |
| ADR alignment           | Pass    | The current stack and implemented source slices remain aligned with ADR 0008, ADR 0009, and ADR 0010. |
| Acceptance tests        | Pass    | The stale frontend coverage assertion was corrected and the acceptance suite now passes. |

### Findings

#### Passed

- The stale acceptance assertion in `tests/acceptance/test_operator_dashboard_ui_acceptance.py` now checks current fallback markers (`mode: 'fallback'`, `Static Baseline`, and `Data mode: Static Baseline`) instead of the obsolete literal `Source Fallback`.
- The CI workflow in `.github/workflows/ci.yml` now uses runnable commands for the current stack: frontend test/build commands that exist today, and Python-based backend verification instead of missing backend npm scripts.
- The previously failing acceptance gate now passes against the current repository state.

#### Gaps / Deviations

- No blocking spec deviations were identified in this post-fix follow-up pass.

### Verdict

- [x] PASS - implementation matches specs
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Keep the acceptance test aligned to documented behavior rather than incidental historical UI wording.
- Consider a separate hygiene pass for generated `__pycache__/*.pyc` files and acceptance `.db` artifacts so future QA runs stay easier to review.

### Verification

- `python -B -m pytest -q -p no:cacheprovider tests/acceptance` -> PASS (`28 passed`)
- `python -m unittest discover -s tests -p "test_*.py"` in `backend/` -> PASS
- `npm test --prefix frontend` -> PASS
- `npm run build --prefix frontend` -> PASS
