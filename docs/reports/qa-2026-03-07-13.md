## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Verify the first Pizza Index backend contract scaffold and the replacement of
the temporary `Behavioral Signals` source placeholder with `Pizza Index
Activity`.

### Specs Reviewed

- `docs/api/openapi.yml`
- `docs/specs/pizza-index-google-maps-activity.md`
- `docs/adr/0009-google-maps-restaurant-activity-for-pizza-index.md`
- `backend/src/routes/pizza_index.py`
- `backend/src/services/pizza_index_pipeline.py`

### Changed Files

- `tests/acceptance/test_openapi_yml_acceptance.py`
- `tests/acceptance/test_initial_draft_project_spec_md_acceptance.py`
- `docs/reports/qa-2026-03-07-13.md`

### Compliance Summary

| Area | Status | Notes |
| --- | --- | --- |
| Pizza Index route scaffold | Pass | The backend now exposes target registry, per-target activity, latest snapshot, and refresh endpoints. |
| Live API documentation | Pass | `docs/api/openapi.yml` now documents the implemented Pizza Index scaffold rather than only the future plan. |
| Source placeholder replacement | Pass | Signal snapshot examples and live responses now use `Pizza Index Activity` instead of `Behavioral Signals`. |
| Regression checks | Pass | Backend tests, frontend tests/build, and acceptance coverage remain green. |

### Findings

#### Passed

- Pizza Index routes exist and return the documented stubbed payload shapes.
- The live OpenAPI contract includes the Pizza Index tag, endpoints, and schema set.
- The signal snapshot source inventory no longer uses the old `Behavioral Signals`
  label.
- The current scaffold is honest about its status: provider fields are `stub`
  and quality is `partial`.

#### Gaps / Deviations

- The Pizza Index scaffold does not yet persist historical Pizza Index snapshots
  or target activity to storage.
- The Google Maps / Puppeteer / SerpApi provider layer is not implemented yet;
  this slice is contract-first and stub-backed only.

### Verification Run Result

- `python -m unittest discover -s tests -p "test_*.py"` -> PASS
- `python -m compileall src` -> PASS
- `npm test --prefix frontend` -> PASS
- `npm run build --prefix frontend` -> PASS
- `python -B -m pytest -q -p no:cacheprovider tests/acceptance` -> PASS

### Verdict

- [x] PASS - implementation matches the scoped Pizza Index scaffold task
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Next implementation slice should add persistence for Pizza Index snapshots and
  per-target activity history.
- After that, wire the first real provider path behind the scaffolded contract.
