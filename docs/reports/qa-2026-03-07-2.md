## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Re-run the QA compliance pass after the backend `SignalSource` contract fix and
issue an updated verdict.

### Specs Reviewed

- `docs/specs/initial-draft-project-spec.md`
- `docs/specs/README.md`
- `docs/api/openapi.yml`
- `docs/adr/0008-predict-strike-mvp-scope-and-initial-stack.md`

### Compliance Summary

| Area                    | Status | Notes |
| ----------------------- | ------ | ----- |
| Functional requirements | Pass   | The current backend scaffold covers the narrow MVP API slice adopted for this implementation pass: health, signal snapshot, explainable scoring, and market comparison. Broader collectors, alerts, and dashboard work remain planned future milestones rather than current deviations. |
| API contract            | Pass   | `GET /api/v1/signals/latest` now matches the documented `SignalSource` schema, and the remaining scaffold endpoints align with `docs/api/openapi.yml`. |
| ADR alignment           | Pass   | Backend stack and endpoint surface remain consistent with ADR 0008. |
| Acceptance tests        | Pass   | Backend and spec-traceable acceptance tests both pass after the contract fix. |

### Findings

#### Passed

- `backend/**` exposes the MVP scaffold routes recorded in `docs/adr/0008-predict-strike-mvp-scope-and-initial-stack.md`.
- `POST /api/v1/risk/score` returns an explainable weighted score with breakdown and thresholds, matching the transparent-scoring direction in `docs/specs/initial-draft-project-spec.md`.
- `GET /api/v1/signals/latest` now returns `SignalSource` items with only `name`, `status`, and `last_checked_at`, and uses documented statuses only.
- `GET /api/v1/markets/opportunities` returns model-vs-market comparison outputs consistent with the current scaffold scope.
- Spec-traceable acceptance tests continue to guard the current MVP surface for `docs/specs/initial-draft-project-spec.md` and `docs/api/openapi.yml`.

#### Gaps / Deviations

- None observed within the scoped backend/API surface for this pass.

### Verdict

- [x] PASS - implementation matches specs
- [ ] FAIL - deviations listed above must be resolved before merge

### Recommended Actions

- Keep `tests/acceptance/test_openapi_yml_acceptance.py` as a guardrail for future backend response-shape changes.
- Add acceptance coverage for alert-oriented behavior once the backend exposes an alerting endpoint or workflow.
- Re-run this QA flow whenever the OpenAPI contract or backend MVP routes change.
