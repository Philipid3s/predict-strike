## QA Report - 2026-03-07

Prepared By: qa-agent

### Task Reference

Expose which upstream actually supplied market data for
`/api/v1/markets/opportunities`.

### Specs Reviewed

- `docs/api/openapi.yml`
- `README.md`

### Compliance Summary

| Area | Status | Notes |
| --- | --- | --- |
| Functional requirements | Pass | Market opportunities now expose explicit upstream provenance as `gamma`, `pizzint`, or `bootstrap`. |
| Backend tests | Pass | Collector and pipeline tests cover all three upstream states. |
| Acceptance tests | Pass | OpenAPI-traceable contract tests were updated and remained green. |
| Documentation | Pass | API docs and README describe the new field and fallback order. |

### Findings

- `backend/src/collectors/polymarket.py` now tags each observation with the
  actual upstream used.
- `backend/src/services/market_pipeline.py` passes that upstream through the
  API response without changing the existing opportunity item shape.
- `tests/acceptance/test_openapi_yml_acceptance.py` now asserts the new
  top-level `upstream` field and its allowed values.

### Verification

- `python -m unittest discover -s tests -p "test_*.py"` in `backend/`
- `python -m compileall src` in `backend/`
- `python -B -m pytest -q -p no:cacheprovider tests/acceptance` from repo root

### Verdict

- [x] PASS
- [ ] FAIL

### Recommended Actions

- If you want the operator dashboard to surface the same provenance, extend the
  frontend market panel to render the `upstream` field beside the existing
  `source.mode` badge.
